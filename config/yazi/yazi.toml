[mgr]
show_hidden = true

[opener]
edit = [
    { run = 'nvim "$@"', block = true,  desc = "Edit with Neovim" }
]

[open]
rules = [
    { mime = "text/*", use = "edit" },
    { name = "*", use = "edit" }
]

